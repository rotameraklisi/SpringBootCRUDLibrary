# SpringBootCRUDLibrary

Projede bir File oluştur adı Dockerfile şeklinde yaz içine mvn package / install et 
dosyanın olduğu konuma git 

docker build -t library:latest .
docker images
docker run -p 8081:8080 library
